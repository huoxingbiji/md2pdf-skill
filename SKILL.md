---
name: md2pdf
description: 将 Markdown 转换为适合交付的 PDF，支持中文排版、代码语法高亮、可点击目录、KaTeX 公式、Mermaid 矢量图、水印与权限保护，并要求渲染后进行页面质量检查。适用于技术文档、方案、报告和流程图文档；不用于编辑 PDF 原稿。
---

# Markdown 转 PDF

将 Markdown 转换为清晰、稳定、可复检的 A4 PDF。转换必须保持源 Markdown 不变；任何语法修复、公式预处理、目录插入或分页指令都只能作用于内存或临时副本。

## 工作流程

1. 定位用户指定的 `.md`。未指定文件且目录中只有一个候选时直接使用；有多个候选且无法从上下文判断时再询问。
2. 读取源文件并统计行数、标题、围栏代码、Mermaid、数学公式、表格和图片。先检查未闭合围栏、未配对公式、加粗标记内部空格等问题。
3. 尊重用户对目录的明确选择。用户未说明、文档超过 500 行且没有目录时，先询问一次；不要自行决定添加目录。
4. 使用本 Skill 目录中的 `convert.mjs` 转换。默认启用安全 Markdown 修复、代码语法高亮、KaTeX 和 Mermaid 自适应布局；修复只存在于转换过程。生成目录时，目录条目必须是可点击的 PDF 内部跳转链接。
5. 将正式 PDF 放入用户指定目录。用户要求“按日期放置”时使用源目录下的 `YYYY-MM-DD` 文件夹，并保持文件名稳定。重新生成时覆盖同名目标；只有目标被占用时才接受 `_new.pdf` 回退，并明确告知。
6. 使用 `scripts/qa_pdf.py` 将最新版 PDF 渲染为 PNG，检查全部页面联系表，并高清检查首尾页、公式页、图表页、表格页和分页临界页。发现截断、重叠、异常空白或不可读内容时，调整后重新生成并复检。
7. 清理临时 Markdown、HTML、字体缓存、页面 PNG 和测试文件，仅保留源文件与最终交付物。

## 基本转换

从包含本文件的 Skill 目录运行：

```powershell
node .\convert.mjs "D:\path\document.md" "D:\path\output.pdf" --toc off
```

常用选项：

- `--toc off|on|auto`：默认 `off`。只有用户同意后才使用 `on` 或 `auto`。
- `--highlight auto|off`：默认 `auto`。优先使用代码围栏声明的语言；未声明时只在识别置信度足够时自动着色。
- `--math auto|off`：默认 `auto`，渲染 `$$...$$` 和可信的 `$...$`。
- `--repair-markdown safe|off`：默认 `safe`，修复加粗标记内部多余空格，不改源文件。
- `--diagram auto|normal|full-page`：默认 `auto`，复杂 Mermaid 自动独占整页。
- `--date-folder`：未显式指定输出 PDF 时写入当天日期文件夹。
- `--watermark "FOX-ESS"`：把水印合并进每页内容流。
- `--owner-password-env MD2PDF_OWNER_PASSWORD`：从环境变量读取所有者密码并限制修改；不要把密码直接写进命令行或文档。

仅在需要高级布局、水印或排错时读取 [references/options.md](references/options.md)。

## 文档预检规则

- 围栏代码和行内代码中的 `$`、`**`、Mermaid 字样不参与公式或语法修复。
- 未闭合代码围栏或未配对 `$$` 属于阻断错误，先修复临时副本再转换。
- 加粗标记内部的首尾空格可安全修复，例如 `**文字： **` 转为 `**文字：**`。
- 对可能改变语义的 Markdown 问题只报告，不擅自修复。
- 标题、目录和 Mermaid 统计不得把代码块中的内容计算在内。

## 公式规则

- 使用 KaTeX 输出矢量文字，并等待公式字体加载完成后再打印。
- 代码块与行内代码中的公式样文本必须保持原样。
- KaTeX 解析失败时停止交付并报告原公式；不能把原始 LaTeX 当成成功结果。
- 质检时检查 PDF 文本中是否意外残留 `$$`、`\\frac`、`\\times` 等标记。

## 代码高亮与目录链接

- 非 Mermaid 围栏代码默认启用语法高亮。已声明语言但解析器不支持时按纯文本渲染并给出警告，不能因此丢失或改写代码。
- 未声明语言的代码块仅在内容长度合理且自动识别置信度足够时着色；短片段和低置信度结果保持纯文本，避免误配色。
- 代码标签、关键字、字符串、数字、注释和函数等使用打印友好的不同颜色；质检时至少高清检查一个有代表性的代码页。
- 转换器为标题生成稳定且去重的锚点。临时生成的目录使用内部链接指向这些锚点，HTML 阶段若发现链接目标缺失应停止转换。
- 含生成目录的 PDF 使用 `qa_pdf.py --expect-internal-links` 验证 PDF 中确实存在内部链接注释；没有链接时该选项会直接失败。文档没有合法的代码示例标记等预期命中时可同时使用 `--strict`。

## Mermaid 与分页规则

- Mermaid 必须生成 SVG；SVG 数量与 Mermaid 块数量不一致时转换失败。
- 默认移除固定 `55vh` 限制。复杂图使用整页模式，图后内容从下一页开始。
- 单个图可在围栏信息字符串中加入 `full-page` 或 `normal` 覆盖全局模式，例如 ` ```mermaid full-page `。
- 用户要求区分路线或环节颜色时，在临时副本中添加 Mermaid `classDef` / `class`，保持源文件不变，除非用户明确要求修改源文件。
- 长表格允许跨页并重复表头；短表格、短代码块和引用块尽量整体保留。不要对所有内容统一使用 `break-inside: avoid`。

## 水印与权限保护

水印和所有者密码是可选后处理。水印应合并到每页内容中，再使用 AES-256 所有者密码限制修改和批注，同时允许正常打开。PDF 权限控制不能从密码学上保证水印永远无法移除；对具备专业工具的人只能提高移除成本。向用户准确说明这一限制。

## 质量门禁

至少确认：

- PDF 存在、非空、页数大于零且为预期纸张；
- Mermaid 全部为 SVG，公式字体已加载，图片无 404；
- 无意外的原始 Markdown/LaTeX 残留，无异常黑块、乱码、截断和重叠；
- 页眉页脚、页码、表格跨页、整页图表和章节衔接正常；
- 代码高亮可辨识且不影响换行；生成目录的内部链接数量大于零并能指向章节；
- 全页联系表无明显异常，重点页高清检查通过；
- 源 Markdown 的大小和修改时间未被转换流程改变。

完成后告知用户最终路径、页数、大小、公式、Mermaid、代码高亮和目录链接数量，是否含目录/水印/加密，以及质检结论。
