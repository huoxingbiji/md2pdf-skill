# 安全政策

## 支持范围

| 版本 | 状态 |
|---|---|
| 1.2.x | 支持安全修复 |
| < 1.2 | 不再主动维护 |

## 私密报告漏洞

请使用 GitHub 的[私密安全公告入口](https://github.com/huoxingbiji/md2pdf-skill/security/advisories/new)报告漏洞。

不要在公开 Issue 中发布以下内容：

- 可直接利用的攻击步骤；
- 密码、令牌或本地敏感路径；
- 未脱敏的 Markdown、PDF 或日志；
- 能读取预期目录以外文件的完整利用样例。

报告中请包含受影响版本、操作系统、风险说明、最小复现和建议修复方向。维护者会先确认收到，再评估影响和发布方案。

## 安全边界

- 转换器在本机启动只监听 `127.0.0.1` 的临时 HTTP 服务；
- 浏览器请求只允许访问该临时服务、`data:` 和空白页；
- 本地图片只允许从 Markdown 所在目录及其子目录读取；
- Mermaid 使用严格安全级别；
- 内容安全策略阻止未授权脚本和外部资源；
- 所有者密码通过环境变量读取；
- 临时 PDF 在成功或失败后尽力删除。

## 不受信任的 Markdown

Marked 不负责清理原始 HTML。虽然本项目通过内容安全策略、请求拦截和文件路径限制降低风险，原始 HTML 和内联样式仍可能改变布局、隐藏内容或制造误导性页面。

只转换可信或已经审阅的 Markdown。处理来自外部人员的文件时，建议在隔离环境运行，并在交付前查看全部页面联系表。

## PDF 权限限制

AES-256 所有者密码和内容流水印能提高普通编辑器修改、批注和页面重组的门槛，但不能保证具备专业工具的人员无法截图、重建页面或移除视觉元素。

不要把 PDF 权限设置描述为不可破解的数字版权保护。

## 密钥处理

- 不要把真实密码写进命令行参数、源代码、Markdown 或 Git；
- 使用短生命周期环境变量；
- 转换结束后删除该变量；
- 不要在 Issue、CI 日志或测试夹具中提交密钥。
